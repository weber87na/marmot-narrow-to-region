`alt + n` => `開啟 narrow 模式`

`alt + v` => `關閉 narrow 模式`

`f5 => debug`

```
npm install -g yo generator-code @vscode/vsce
vsce package
code --install-extension marmot-narrow-to-region-0.0.1.vsix
```


```
npm install
npm run compile
vsce package
```